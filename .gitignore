# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ultra-orchestration run artifacts (plan, build log, findings, verdicts).
# Kept out of history; a curated summary goes into docs/ at the end.
.pipeline/

# Real patient exports must never be committed (council iteration 1, D1).
# Only the tracked synthetic samples are exempt from this rule.
*.csv
!public/samples/*.csv
!public/sample-visits.csv
